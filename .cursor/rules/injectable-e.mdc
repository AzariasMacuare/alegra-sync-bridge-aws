---
alwaysApply: true
---

@Injectable()
export class AiService {
  openai: OpenAI;

  constructor(
    @Inject(AppEnvsConfig.KEY)
    private configService: ConfigType<typeof AppEnvsConfig>,
  ) {
    this.openai = new OpenAI({ apiKey: this.configService.openAiKey });
    console.log(this.openai);
  }
}
